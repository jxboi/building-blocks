# Secrets / local environment — .env.example is committed, .env is not.
.env
.env.local
.env.*.local

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Per-app artifacts are ignored by apps/*/.gitignore (node_modules, bin/, obj/, …).
